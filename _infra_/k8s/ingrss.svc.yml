apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-service-1
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true" # this allows regex in paths (if needed)
spec:
  rules:
    - host: service-1.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: service-1-svc
                port:
                  number: 3000 # must match the port (Not the target port) defined in service1.depl.yaml
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: service-1-svc # must match the service name defined in service1.depl.yaml
                port:
                  number: 3000
          - path: /users
            pathType: Prefix
            backend:
              service:
                name: service-1-svc # must match the service name defined in service1.depl.yaml
                port:
                  number: 3000
  ingressClassName: nginx


---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-service-2
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
    - host: service-2.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: service-2-svc # must match the service name defined in service2.depl.yaml
                port:
                  number: 5001 # must match the target port defined in service2.depl.yaml
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: service-2-svc # must match the service name defined in service2.depl.yaml
                port:
                  number: 5001 # must match the target port defined in service2.depl.yaml
          - path: /api/users/?(.*)/orders
            pathType: ImplementationSpecific
            backend:
              service:
                name: service-2-svc # must match the service name defined in service2.depl.yaml
                port:
                  number: 5001 # must match the target port defined in service2.depl.yaml
  ingressClassName: nginx

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-fe-client
  annotations:
    kubernetes.io/ingress.class: nginx
    # nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
    - host: fe-client.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fe-client-svc # must match the service name defined in fe-client.depl.yaml
                port:
                  number: 80 # must match the port (Not the target port) defined in fe-client.depl.yaml
  ingressClassName: nginx